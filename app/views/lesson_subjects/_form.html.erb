<%= form_for(lesson_subject) do |f| %>
  <% if lesson_subject.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(lesson_subject.errors.count, "error") %> prohibited this lesson_subject from being saved:</h2>

      <ul>
      <% lesson_subject.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :lesson_id %>
    <%= f.number_field :lesson_id %>
  </div>

  <div class="field">
    <%= f.label :subject_id %>
    <%= f.number_field :subject_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
