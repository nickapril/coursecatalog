<!-- None of this is displaying -->

<% if @enrollments.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@enrollments.errors.count, "error") %> prohibited this enrollment from being saved:</h2>
 
    <ul>
    <% @enrollments.errors.full_messages.each do |msg| %>
      <li><%= msg %></li>
    <% end %>
    </ul>
  </div>
<% end %>